import { FiterCodeLengthPipePipe } from './fiter-code-length-pipe.pipe';

describe('FiterCodeLengthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiterCodeLengthPipePipe();
    expect(pipe).toBeTruthy();
  });
});
